---
title: "Connect Okta"
description: "Connect Okta to populate identity and device inventory in Beacon."
---

## Overview

Connect Okta from the Beacon dashboard to automatically populate the users, groups, and devices tabs.

## Connect Okta

1. Log in to the Beacon dashboard.
2. Go to **Connectors**.
3. Find the Okta integration.
4. Click **Connect**.
5. Enter your Okta org URL.
6. Enter your Okta API token.

<Frame caption="Use the Okta connector setup flow in the Beacon dashboard to enter your Okta org URL and API token.">
  <img src="/images/asymptote_dashboard_okta_connector.png" alt="Beacon dashboard Okta connector setup form." />
</Frame>

After the connector is authorized, Beacon automatically syncs Okta data into the users, groups, and devices tabs in the dashboard.

## Generate an Okta API token

Create the Okta API token from the Okta Admin Console:

1. Go to **Security**.
2. Open **API**.
3. Select **Tokens**.
4. Click **Create token**.

<Frame caption="Generate the API token from the Okta Admin Console before returning to the Beacon connector setup.">
  <img src="/images/okta_token_generation.png" alt="Okta Admin Console API token generation screen." />
</Frame>

Copy the generated token and paste it into the Okta connector setup flow in Beacon.
